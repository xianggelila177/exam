# exam

这是一个 Agent Skill 仓库，Skill 本体位于：

```
exam-print-prep/
├── SKILL.md
└── scripts/
    └── prep_print.py
```

请按标准 Skill 目录使用：让客户端加载 `exam-print-prep/` 目录，而不是把仓库根目录
当成 Skill 根目录。这样 `SKILL.md` 中的 `name: exam-print-prep` 与父目录名一致。
